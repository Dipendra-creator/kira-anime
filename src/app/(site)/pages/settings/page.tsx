import Head from "next/head";

const Settings = () => {
  return (
    <div>
      <Head>
        <title>Settings</title>
      </Head>
      <h1>Settings</h1>
    </div>
  );
};

export default Settings;
